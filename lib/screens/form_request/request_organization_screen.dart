import 'package:flutter/material.dart';

class RequestOrganizationScreen extends StatelessWidget {
  const RequestOrganizationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
